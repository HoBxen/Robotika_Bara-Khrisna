cd ~/Downloads/repository

Perintah untuk berpindah ke direktori repository di folder Downloads

catkin_make

Perintah untuk membangun (build) package ROS dalam workspace yang menggunakan sistem build catkin

source devel/setup.bash

Perintah untuk mengaktifkan environment ROS dalam workspace saat ini

roslaunch mastering_ros_robot_description_pkg view_demo.launch

Menjalankan file launch untuk menampilkan visualisasi robot demo dalam RViz
Package yang digunakan adalah mastering_ros_robot_description_pkg

roslaunch mastering_ros_robot_description_pkg view_arm.launch

Menjalankan file launch untuk menampilkan visualisasi lengan robot (robot arm) dalam RViz

roslaunch mastering_ros_robot_description_pkg view_mobile_robot.launch

Menjalankan file launch untuk menampilkan visualisasi robot bergerak (mobile robot) dalam RViz
